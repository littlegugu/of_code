#include <stdio.h>
#include <string.h>

char* replace(char *str){
    char *strReplace = NULL;
    
    for (int i = 0; i < strlen(str); i++)
    {
        if (str[i]==' ')
        {

        }
    }
    return str;
}

main(int argc, char const *argv[])
{
    char str[] = "We are happy.";
    replace(str);

    getchar();
    return 0;
}